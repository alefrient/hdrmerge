name: Windows Build

on:
  workflow_dispatch:
    inputs:
      build-type:
        description: 'Build type'
        required: true
        default: 'Release'
        type: choice
        options:
          - Release
          - Debug
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Install build tools
      run: |
        choco install -y cmake --installargs 'ADD_CMAKE_TO_PATH=System'
        choco install -y visualstudio2022-buildtools --package-parameters "--add Microsoft.VisualStudio.Workload.NativeDesktop --includeRecommended"
        
    - name: Install dependencies via vcpkg
      run: |
        git clone https://github.com/microsoft/vcpkg.git
        .\vcpkg\bootstrap-vcpkg.bat
        .\vcpkg\vcpkg install exiv2 libtiff libpng libraw[lcms] qt5-base
        
    - name: Setup environment
      run: |
        echo "VCPKG_ROOT=$PWD\vcpkg" | Out-File -FilePath $env:GITHUB_ENV -Append
        echo "PATH=$PWD\vcpkg\installed\x64-windows\bin;$env:PATH" | Out-File -FilePath $env:GITHUB_ENV -Append
        
    - name: Configure with CMake
      run: |
        mkdir build
        cd build
        cmake .. -G "Visual Studio 17 2022" -A x64 `
          -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT\scripts\buildsystems\vcpkg.cmake" `
          -DCMAKE_BUILD_TYPE=${{ github.event.inputs.build-type || 'Release' }}
      
    - name: Build
      run: |
        cd build
        cmake --build . --config ${{ github.event.inputs.build-type || 'Release' }} --target install
      
    - name: Package artifacts
      run: |
        mkdir artifact
        copy build\${{ github.event.inputs.build-type || 'Release' }}\hdrmerge.exe artifact\
        copy $env:VCPKG_ROOT\installed\x64-windows\bin\*.dll artifact\
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: hdrmerge-windows
        path: artifact/*
        if-no-files-found: error
